# The domain boundary. Everything SSH-specific that isn't a node lives here.
# The engine (src/star_actually, templates/, assets/) never mentions the domain.

title: "SSH, Actually"
system_name: "*, Actually"
domain_word: "SSH"
tagline: "100% signal, 0% noise."
prompt: "What do you want to know about SSH, actually?"
url: "https://star-actually.sageframe.net/ssh-actually"
root_node: "what-ssh-is"
repo: "https://github.com/sageframe-dharma/star-actually"
author: "Andrew T Marcus"
receptionist: false  # static build; the /ask backend is not deployed
base_path: "/ssh-actually"
